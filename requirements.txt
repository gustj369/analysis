# Stock Analysis System dependencies
# Install: pip install -r requirements.txt
#
# [FIX 2차 A-1] Python 버전 요구사항 명시
#   - X | Y union 타입, list[dict] 소문자 제네릭 → Python 3.10+ 필요
#   - Python 3.14 권장 (CLAUDE.md 기준)
#   - Python 3.10 미만 환경에서는 TypeError 발생

# Python >= 3.10 required

yfinance>=0.2.0
finance-datareader>=0.9.50
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.0.0
