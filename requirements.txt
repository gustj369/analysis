# Stock Analysis System dependencies
# Install: python -m pip install -r requirements.txt
#
# Python 3.10+ is required because the code uses modern type syntax
# such as X | Y unions and built-in generic types like list[dict].

yfinance>=0.2.0
finance-datareader>=0.9.50
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.0.0
